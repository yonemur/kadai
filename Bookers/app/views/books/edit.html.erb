<h1>Editing Book</h1>

<%=form_with model: @book,ulr:book_path(@book.id),method: :patch do |f|%>

    <% if @book.errors.any?%>
     <div class="error">
       <span class=error_text><%=@book.errors.count %>errors prohibited this book from being saved:</span>
       <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
    　　<% end %>
       </ul>
     </div>

    <%end%>

    <h4>Title</h4>
    <%=f.text_field :title%>

    <h4>body</h4>
    <%=f.text_area :body%>

    <span class="button"><%=f.submit'Update Book'%></span>

    <%=link_to "show",book_path(@book.id)%>

    |

    <%=link_to "Back",books_path%>
<%end%>